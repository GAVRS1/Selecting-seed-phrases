import { NETWORKS_CONFIG } from './networks';

export const CONFIG = {
  bitcoin: {
    RPC_URLS: NETWORKS_CONFIG.bitcoin.RPC_URLS,
    NAME: NETWORKS_CONFIG.bitcoin.NAME,
    NATIVE_CURRENCY: NETWORKS_CONFIG.bitcoin.NATIVE_CURRENCY,
  },
};
